# Env
.env
.env.*
!.env.example
supabase/.env

# Expo
.expo/
dist/
web-build/

# Vercel (roxy-web static deploy links live inside dist/, but never commit one)
.vercel/

# Native
ios/
android/
*.orig.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision

# Node
node_modules/

# Supabase
supabase/.branches
supabase/.temp


# Claude Code local settings (machine-specific)
.claude/settings.local.json

# MCP config (contains secrets)
.mcp.json
.superpowers/

# Git worktrees
.worktrees/

# Local agent/tooling artifacts and design handoffs — never commit
.agents/
.codex/
.playwright-mcp/
.superpowers/
docs/Roxy-handoff/
/*.png
AGENTS.md

# Impeccable critique snapshots (tool output, not source)
.impeccable/
